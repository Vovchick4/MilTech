# Урок №120. Введення в ООП

Об'єктно-орієнтоване програмування (ООП) — парадигма, де програма будується з об'єктів, що об'єднують дані та поведінку.

## Основні концепції

| Концепція | Опис |
|-----------|------|
| **Клас** | Шаблон для створення об'єктів |
| **Об'єкт** | Екземпляр класу |
| **Інкапсуляція** | Приховування деталей реалізації |
| **Наслідування** | Створення класів на основі інших |
| **Поліморфізм** | Однаковий інтерфейс для різних типів |

## Процедурне vs ООП

```c++
// Процедурний підхід:
struct Student { std::string name; int grade; };
void printStudent(Student s) { ... }

// ООП підхід:
class Student {
    std::string name;
    int grade;
public:
    void print() { ... }  // метод (поведінка)
};
```

## Переваги ООП

1. **Модульність** — класи як незалежні компоненти
2. **Повторне використання** — наслідування
3. **Легкість підтримки** — зміни локальні
4. **Безпека** — інкапсуляція приховує деталі

## Приклад

```c++
#include <iostream>
#include <string>

class Dog
{
    std::string name;
    int age;
public:
    Dog(std::string n, int a) : name{n}, age{a} {}
    
    void bark() const {
        std::cout << name << " каже: Гав!\n";
    }
    
    int getAge() const { return age; }
};

int main()
{
    Dog rex("Рекс", 3);
    rex.bark();
    return 0;
}
```

## Чотири "стовпи" ООП

1. **Абстракція** — робота з сутностями, а не деталями
2. **Інкапсуляція** — приховування реалізації
3. **Наслідування** — "is-a" відношення
4. **Поліморфізм** — єдиний інтерфейс

## Правило

ООП — це спосіб мислення. Клас має моделювати реальну сутність або концепцію.

## Тест

1. Які основні концепції ООП?
2. Чим клас відрізняється від об'єкта?
3. Що таке інкапсуляція?
